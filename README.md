# nlp-dashboard/README.md
# Enterprise Customer Sentiment & Text Analytics Dashboard

An end-to-end, production-grade NLP application utilizing **Streamlit**, **Scikit-learn**, and **Plotly** to process and reveal customer reviews insights dynamically.

## 🚀 Quickstart Guide

### 1. Environment Setup
Clone this repository tree locally into your preferred workspace. Ensure Python 3.10+ is installed, then set up a virtual environment and update dependencies:

```bash
python -m venv venv
source venv/bin/activate  # On Windows use: venv\Scripts\activate
pip install -r requirements.txt